# Karma Worker

Projects moderated contribution outcomes into the immutable karma ledger and leaderboard snapshots.

Runtime entrypoint lives in `src/index.ts` and delegates to the API worker contract.
