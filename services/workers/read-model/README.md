# Read Model Worker

Projects canonical listing, collection, and trust events into feed, map, detail, and search read models.

Runtime entrypoint lives in `src/index.ts` and delegates to the API worker contract.
